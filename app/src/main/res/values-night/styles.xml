<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="android:statusBarColor">@color/bgColor</item>
    </style>

    <style name="NumText" parent="@style/Widget.Neumorph.ImageButton">
        <item name="android:textColor">@color/numText</item>
        <item name="android:textSize">28sp</item>
    </style>
    <style name="ButtonStyle" parent="@style/Widget.Neumorph.ImageButton">
        <item name="neumorph_shadowColorLight">@color/shadowColorLight</item>
        <item name="neumorph_shadowColorDark">@color/shadowColorDark</item>
        <item name="backgroundTint">@color/bgColor</item>
    </style>
</resources>